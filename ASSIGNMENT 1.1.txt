1.Say the true or false for the below statements:
  a)Prescriptive analysis used to predict the future outcomes?
      True
  b)Base R packages installed automatically?
      True
2.What is recycling of elements in a vector?
      In R programming language,recycling of elements is when we perform oprations on two different vectors having different lengths.In it, the elements of the shorter length vector are used to complete the       operation.
3.Give an example of recycling of elements?
     1)  v1<-c(4,1,0,6)
          v2<-c(2,4)
          print(v1*v2) 
         
          [1]- 8,4,0,24